# 更新日志

安装包在公众号「**半颗花菜**」，发送「时光档案」获取。装新版本直接覆盖安装即可，数据不会丢。

---

## 1.0.9 · 2026-09-03

**修复**

- **回收站里把父层级收起来之后，「已选 N 项」里有一部分是你看不见的**。折叠只影响显示、不会取消勾选，所以那个数字里可能含着被收起来的动态，而「删除选中」不能撤销。现在三处一起说清楚：折叠的那一行标出「内含 N 项已选」、底部补一行「其中 M 项在折叠的层级里」并可一键展开、删除前的确认框按档案／时间线／动态分类列出勾了什么，并点出哪些藏在折叠里

## 1.0.8 · 2026-09-02

**新增**

- **档案与时间线的头像也会备份到云端了**。此前头像完全不参与同步，换手机或重装之后，档案列表里的人全变回一个首字母。现在它跟着「图片同步」开关一起走，换了头像下次同步云端也会更新
- 日历回顾里，选中的那天没有记录时，可以一键**跳到最近有记录的一天**（前后都能翻，翻到头会绕回另一端）
- 「填写初始点」的弹窗补上了描述栏——初始点往往是最需要写清由来的那一条，以前只能建完再进编辑页补

**改动**

- **回收站改成「档案 → 时间线 → 动态」的折叠树**。以前是三段扁平列表，看不出一条动态原来属于谁。现在层级展开，还没被删的父级会以虚线框出现，只作归属说明、不能从那儿删除
- **恢复一条动态时，它的时间线和档案会跟着回来**。以前恢复完，它挂在一个界面上查不到的父级下面，点了「恢复」却在任何地方都找不到。注意只捞回父级那几行，同一条时间线里其他还在回收站的动态不受影响
- **「仅下载」现在会清空本机回收站**，确认弹窗里已写明。这个入口的意思是「让这台手机等于云端」，而云端备份里带着删除记录，照搬过来会让你在一台自己从没删过东西的手机上，看到一堆别的设备删掉的记录
- **档案名不能与已有的重复**；档案名限 20 字、备注 200 字，动态标题 30 字、描述 1000 字。重名是边输入边提示（输入框描红并说出撞了哪个名字），不再常驻一句说明文字
- 「我的」页开头三张卡归到「基本信息」标题下；档案超过 4 个时列表在自己容器内滚动，不再把下面的锁、回看、备份等入口整片推到屏外
- 日历回顾里选中日没有记录时，那块面板撑满剩余空间，不再在屏幕中间留一片无主的空白

**修复**

- **删除档案后，它仍然留在「我的」页列表里**，看着像删除没生效
- 同上一处的连带问题：回收站里的「恢复档案」点了没反应；已删除的档案仍被当成「我」，既显示在顶部又删不掉
- **删除档案后底部不弹撤回条**，只有一句「已删除」，没有反悔的入口
- 同步报错「t.filter is not a function」（大图分片上传那条路径），并且分片的增量判断此前一直失效、每次都在全量重传
- 时间轴上长按动态展开菜单后，点卡片左右两侧的空白收不起来
- 编辑档案/时间线/动态时，头像路径失效的地方会退回首字母占位，不再留一块无法解释的空白

## 1.0.7 · 2026-09-01

**同步与备份页重做**

填好了服务器和账号、点了同步、显示成功——但照片一张都没传上去。原因是「图片同步」的开关默认是关着的，而它和同步按钮挤在同一页同一层级，很容易漏掉。光把说明写详细防不住，所以这版把页面拆开了：

- **上半是「配置」，下半是「执行」，中间隔一份就绪清单**。清单会在你点同步之前，把当前配置状态复述一遍——哪些没配完、哪些配了但可能不是你想要的。
- **清单每一行都能点**，直接跳到该处理的地方，不会只告诉你「请先测试连接」却不给去处。
- **没测过连接不让点同步**。而且测试结果记着你当时填的是哪个账号，改过服务器地址或密码之后旧结果自动作废——避免填错了新地址还能一路点到「仅上传」，然后在半路失败。
- **「仅下载」不再追问图片上传开关**：那个开关管的是「我的照片要不要上云」，取回照片跟它没关系。
- **改云端加密口令时要先输一遍原口令**。不是防别人，是防手滑：这一栏改错了，云端那份备份就会用一个你自己都不知道的口令重新加密，而它无法找回。
- 不开加密的人不会再被当成「没配完」，也不会每次同步都多问一次。

## 1.0.6 · 2026-09-01

**修复**

- 主页「其他时间线」和档案页里的时间线头像不显示
- 编辑时间线时，头像的占位圆圈被画成了椭圆
- 首次安装走完新手引导后，直接进入档案编辑页填起始时间，不再让人自己找入口

**改动**

- 时间轴上长按动态卡片，菜单精简到只剩「删除」；点删除按钮以外的任意位置都能收起
- 时间轴删除动态改成从底部弹确认框，确认后列表停在原处，不再跳回顶部

## 1.0.5 · 2026-09-01

**新增**

- 「我的」页的最近动态可以按档案筛选，默认看全部档案
- 时间轴顶部的跨度条与下方卡片联动：点跨度条会高亮对应的那张卡

**改动**

- 日历回顾：当月只画到今天（未来的空格子不再占位）、每周从周日开始、跳转入口挪到顶部、选中的那天没有记录时收成一行，不再撑开一块空白
- 长按菜单点任意空白处即可收起

## 1.0.4 · 2026-08-31

**新增**

- 时间轴卡片长按可展开「编辑 / 删除」

**改动**

- 日历回顾改成上下连续滚动，滑到 1 月往上、12 月往下不再顶死

**修复**

- 自定义调色盘拖动跟手了，并且整块区域都能滑；主色与背景接近时文字不再看不见

## 1.0.3 · 2026-08-31

**改动**

- 时间跨度总览：左上角固定显示当前所在的年月；刻度随缩放自动换粒度——缩到一年以内标月份、一个月以内标日期
- 跨度区域内部可以上下滚动，竖着滑不再被转发给下方的时间轴

**修复**

- 当天的时间点标签被裁掉
- 跨度区滚动后，年份竖线的高度没跟着内容变

## 1.0.2 · 2026-08-30

**新增**

- 时间线可以设置头像（不设置的话保持首字符 + 点缀色）
- 卡片流里的时间段动态会显示时长

**改动**

- 编辑表单：填了截止日期后开始日期会跟着走、初始点的必填项标了星号、头像裁剪后要确认才保存
- 时间轴列表撑到屏幕底部；只有卡片本身可点；时间点不再显示「时长 当天」这种废话

**修复**

- 时间跨度总览大改版：支持双指缩放、时间点用橙色竖线区分、修掉被顶部导航遮挡与滚动时位置偏移
- 按住跨度区上下滑动不再连带整页滚动

## 1.0.1 · 2026-08-30

**新增**

- 「我的」页最近动态改成首尾相接的匀速环滚，范围放宽到 10 天 / 15 条，手动滑动会暂停自动滚动
- 「仅上传」时会清理云端多余的照片（删过的动态留在云端的那些）

**改动**

- 档案名称限 20 字、动态标题限 30 字，输入时显示字数
- 必填项统一加红色星号
- 主页主线右侧改成「⋯」点击进编辑
- 删档案纳入回收站（软删除，可恢复，也可彻底删除）
- 主页搜索栏加清空按钮

**修复**

- 从云端恢复时，如果档案正在回收站里会报数据库唯一约束错误
- 主页「同步与备份」显示的时间少了 8 小时（时区没转）
- 编辑主线时不该出现的分类字段

## 1.0.0 · 2026-08-30

首个发布版本。

- **记录**：按时间记下动态——时间点或时间段（结束留空为「至今」）、地点、最多 9 张照片、标题与描述
- **档案**：档案是分类而不是账号，一个人可以同时管自己、孩子、宠物、某个项目
- **三种看法**：时间轴（时间段画成一段「轨」）、照片墙、卡片流，顶部还有一条时间跨度总览
- **时光机**：主页自动翻出往年今天发生过的事
- **时间线锁**：单条时间线设口令，锁上后不出现在搜索、时光机与统计里
- **日历回顾**：纵向滚动的月历 + 热力色块，按日期找回「大概是那阵子的事」
- **统计**：记了多少条、多少天、最长连续多少天、每年每月分布、各时间线占比
- **回收站**：删除先进回收站保留 5 天，另有 5 秒撤回条
- **备份**：WebDAV / Gitee 私有仓库 / 导出文件三种任选，支持端到端加密（AES-256-GCM），可设自动同步
- **外观**：11 个预设主色 + 自定义调色盘，深色模式三态
- **隐私**：没有账号、没有服务器，权限只有相机与网络，不申请定位
